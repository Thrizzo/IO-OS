# KWin (window manager) settings, shipped to /etc/skel/.config/.
# Stub. Will set: window button order matching Windows (min/max/close on
# right), focus-follows-click, no compositor effects that surprise users
# coming from Windows.
