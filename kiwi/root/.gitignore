# Everything in this directory is generated by build.sh's overlay-staging
# step. Source-of-truth files live under branding/ and plasma-config/;
# build.sh copies them here so Kiwi can overlay them onto the image root.
*
!.gitignore
!README.md
