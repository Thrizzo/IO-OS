#!/usr/bin/env bash
# IO Linux — local ISO build entry point.
# Wraps `kiwi-ng system build` against ./kiwi/, output lands in ./out/.
#
# Stub: minimal Kiwi config lands in the next commit; this script will
# then become functional. For now it prints a TODO and exits non-zero so
# CI doesn't silently pass.

set -euo pipefail

echo "TODO: build.sh not yet implemented — see docs/architecture.md"
exit 1
